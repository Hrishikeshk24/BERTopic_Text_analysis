# Core
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7

# Topic modeling
bertopic>=0.16
umap-learn>=0.5
hdbscan>=0.8

# Transformer stack (recommended path)
torch>=2.2          # CPU build is sufficient
transformers>=4.40,<5
sentence-transformers>=3.0

# Offline sentiment fallback
vaderSentiment>=3.3

# Gradio scoring app
gradio>=5.0
huggingface-hub>=0.34,<1.0   # gradio may pull hub 1.x, which breaks transformers 4.x
jinja2>=3.1.2                # required by gradio's dataframe component
